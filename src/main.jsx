import React from 'react';
import ReactDOM from 'react-dom/client';
import RTLHSite from '../rtlh-website (2).jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RTLHSite />
  </React.StrictMode>,
);
